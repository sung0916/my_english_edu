# =================================================
# 1. General & OS
# =================================================
.DS_Store
Thumbs.db
*.log
*.tmp

# =================================================
# 2. Build & Dist (빌드 결과물)
# =================================================
build/
out/
dist/
dist-ssr/
web-build/

# =================================================
# 3. IDE & Tools
# =================================================
.idea/
.vscode/
*.iml
*.iws
*.ipr
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
.eslintcache

# NetBeans
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# =================================================
# 4. Backend (Spring Boot / Gradle)
# =================================================
.gradle/
!gradle/wrapper/gradle-wrapper.jar
logs/
*.pid

# !! SECURITY (Backend) !!
# 경로가 바뀌어도 안전하게 잡히도록 **/ 패턴 사용
**/application.yml
**/application-local.yml
**/application-secret.json
**/application-secret.yml
**/application-prod.yml
**/gcp-credentials.json

# =================================================
# 5. Frontend (React / Vite)
# =================================================
# 의존성 모듈 (가장 중요)
node_modules/

# Expo & Metro (혹시 남아있을 찌꺼기 파일 무시)
.expo/
.metro-health-check*

# 타입스크립트 빌드 정보
*.tsbuildinfo
expo-env.d.ts

# Native 폴더 (웹 전용으로 전환했으므로 무시 처리)
/ios
/android

# 테스팅 커버리지
coverage/

# =================================================
# 6. Global Security (환경변수 및 키)
# =================================================
# 로컬 환경 변수 파일 (비밀번호, API 키 등)
.env
.env.local
.env.*.local
*.local

# 인증서 및 키 파일
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# 공통 비밀 키 파일
secrets.json
client_secret.json

# 디버그 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*